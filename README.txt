README\n\nFirst-Try is just an initial attempt to start using github from the terminal in a local machine (or several machines). It will also be used for storing several unrelated c source files and binaries, most likely tutorial exercises. Maybe some ASM and Java too, who knows... whatever xD
